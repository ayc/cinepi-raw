# Track Specification: README rpicam-vid LUT Example

## Goal
To enhance the `README.md` by providing a clear example of using the new LUT post-processing stage with standard `rpicam-apps` utilities like `rpicam-vid`.

## Core Requirements
1.  Add a command-line snippet showing `rpicam-vid` usage with `--post-process-file`.
2.  Explain that the LUT stage is compatible with all standard `rpicam-apps`.
